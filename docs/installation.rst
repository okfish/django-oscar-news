============
Installation
============

At the command line::

    $ easy_install django-oscar-news

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-oscar-news
    $ pip install django-oscar-news
